<?xml version="1.0"?>
<book>
	<bookinfo>
		<title>Clojure</title>
		<author>
			<firstname>Damien</firstname><surname>Radtke</surname>
		</author>
		<author>
			<firstname>Zigmantas</firstname><surname>Kryžius</surname>
		</author>
	</bookinfo>
	<chapter id="overview"><title>Overview</title>
		<para>
			This plugin comes packaged with Clojure, a functional programming language
			targeted for the JVM.
		</para>
		<para>
			The jars provided by this plugin are clojure-1.8.0, clojure-contrib-1.2.0
			and clojure-jsr223-1.2.jar. Different jars can be used by configuring the plugin
			in the Plugin Options window.
		</para>
	</chapter>
	<chapter id="console"><title>Using with Console</title>
		<para>
			If the Console plugin is installed, this plugin automatically sets the
			system environment variable CLOJURE to the path of clojure-core and clojure-contrib.
			So to run a script called 'script.clj', cd to the file's directory (cd $d) and run:
		</para>
		<para><programlisting>
			java -cp "$CLOJURE" clojure.main script.clj
		</programlisting></para>
		<para>
			Alternatively, this plugin installs a clojure commando file, so scripts can
			be run by setting it as the interpreter for clojure files (Plugin Options -> Console
			-> Compile &amp; Run), and then Plugins -> Console -> Run Current Buffer.
		</para>
		<para>
			With the Clojure SE Shell plugin installed, the Clojure script handler is available
			so you can write jEdit macros in Clojure and test them in Clojure script engine shell.
		</para>
	</chapter>
</book>
